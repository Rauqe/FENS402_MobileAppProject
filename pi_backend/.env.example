# Kopyalayın: cp .env.example .env  — gerçek değerleri .env içinde tutun (repoya eklemeyin).
# AWS / API Gateway tabanı (ble_server.py, dispense_controller.py)
API_BASE_URL=https://YOUR_API.execute-api.REGION.amazonaws.com/default

# Kinesis Video (api_server.py ile birlikte sürekli yayın — aws configure --profile dispenser_video)
# KVS_STREAM_ENABLED=0   # yayını kapatmak için
# KVS_RETRY_SEC=30
# AWS_REGION=eu-central-1
# KVS_STREAM_NAME=dispenser-live-feed
# AWS_PROFILE=dispenser_video
